MAJOR=1
MINOR=2
#REVISION=0
PRERELEASE=b5


COMMIT_HASH=$(git rev-parse --short HEAD)$(test -z "$(git status --porcelain)" || echo "+")
VERSION="${MAJOR}.${MINOR}${REVISION:+.$REVISION}${PRERELEASE} ($COMMIT_HASH)"
BUILD_VERSION=0n
